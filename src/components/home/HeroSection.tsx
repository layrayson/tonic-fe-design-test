import NavBar from "../shared/NavBar";

const HeroSection = () => {
  return (
    <section className="h-screen bg-dark-blue bg-hero-arc bg-no-repeat bg-right-top px-100px pt-14">
      <NavBar />
      <div className="flex"></div>
    </section>
  );
};

export default HeroSection;
